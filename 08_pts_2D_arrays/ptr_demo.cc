#include <iostream>
using namespace std;

int main(){
    // int * ptr = NULL;

    // cout << ptr << endl;
    // cout << *ptr << endl; 

    // pointer arithmetic
    // int x = 10;
    // int y = 20;

    // int *px = &x;
    // int *py = &y;

    // cout << px << " " << &px << endl;
    // cout << py << " " << &py << endl;

    // int  add = *px + *py;

    // int arr[100];

    // int *px = &(arr[0]);
    // int *py = &(arr[1]);

    // cout << px - py;

    char * ptr = NULL;
    cout << (int*)ptr + 1;
}